import 'package:flutter/animation.dart';

/// Shared layout constants for slider components.
const double kSliderBannerHeight = 150.0;

/// Horizontal spacing applied between the slider banners.
const double kSliderBannerSpacing = 8.0;

/// Duration used by the auto-scroll animation for the slider banners.
const Duration kSliderAutoScrollDuration = Duration(milliseconds: 650);

/// Curve used by the auto-scroll animation for the slider banners.
const Curve kSliderAutoScrollCurve = Curves.easeInOutCubicEmphasized;